#
# This file contains configuration flags to customize your site
#

# Name of your site (displayed in the header)
name: Luke Hodnett

# Short bio or description (displayed in the header)
description: Developer from Denver

# URL of your avatar or profile pic (you could use your GitHub profile pic)
avatar: https://raw.githubusercontent.com/barryclark/jekyll-now/master/images/jekyll-logo.png

#
# Flags below are optional
#

# Includes an icon in the footer for each username you enter
footer-links:
  dribbble:
  email: connect@lukehod.net
  facebook:
  flickr:
  github: lukehod
  instagram: lukehod
  linkedin:
  pinterest:
  rss: on # just type anything here for a working RSS icon
  twitter:
  stackoverflow: users/4262448/luke-h # your stackoverflow profile, e.g. "users/50476/bart-kiers"
  youtube: # channel/<your_long_string> or user/<user-name>
  googleplus: # anything in your profile username that comes after plus.google.com/


# Enter your Disqus shortname (not your username) to enable commenting on posts
# You can find your shortname on the Settings page of your Disqus account
disqus: blog-rv6vald80o

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate tracking
google_analytics: UA-157263567-1

# Your website URL
# Used for Sitemap.xml and your RSS feed
url: http://lukehod.net

# If you're hosting your site at a Project repository on GitHub pages
# (http://yourusername.github.io/repository-name)
# and NOT your User repository (http://yourusername.github.io)
# then add in the baseurl here, like this: "/repository-name"
baseurl: ""

# This is used to show what part of a post will be shown as an exerpt on the main
# page. Anything below this tag place on the post will be excluded.
excerpt_separator: "<!--more-->"

# This is the directory where images for blog posts are stored
post_image_dir: "/images/posts/"

#
# !! You don't need to change any of the configuration flags below !!
#

permalink: /:title/

# The release of Jekyll Now that you're using
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: :expanded # You might prefer to minify using :compressed

# Set pagination options (for use with jekyll-pagination plugin)
# see: https://jekyllrb.com/docs/pagination/
paginate: 3

# Use the following plug-ins
plugins:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-feed # Create an Atom feed using the official Jekyll feed gem
  - jekyll-paginate # Allow pagination of blog posts
  - jekyll-seo-tag # add metadata tags for search engines and social networks

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME
